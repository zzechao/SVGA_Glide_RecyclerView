gradlePlugin {
    plugins {
        register("build") {
            id = project.pluginId_local
            implementationClass = "com.zhouz.plugin.SVGAGlidePlugin"
        }
    }
}

publishing {
    repositories {
        maven {
            url "$rootDir\\repo\\"
        }
    }
}
